// import express from 'express';
// import { startChat, sendMessage, getChatMessages, markMessagesAsRead, blockUser } from '../controllers/chatController.js';
// import { protect } from '../middleware/authMiddleware.js';

// const router = express.Router();

// router.post('/start', protect, startChat); // Start a private chat
// router.post('/send', protect, sendMessage); // Send a message
// router.get('/:chatId/messages', protect, getChatMessages); // Fetch chat messages
// router.post('/mark-read', protect, markMessagesAsRead); // Mark messages as read
// router.post('/block', protect, blockUser); // Block a user

// export default router;
